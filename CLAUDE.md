# Claude Code 작업 규칙 — Tabilog

## 참조 문서
- 전체 기획/로드맵: `PROJECT_PLAN.md` (항상 참조, 절대 임의로 수정하지 말 것)
- 결정 기록: `DECISION_LOG.md` (매 주요 결정마다 기록)

## 진행 원칙
1. **스코프 고정**: 지금 진행 중인 Phase 외의 기능은 절대 먼저 구현하지 않는다.
   Phase는 사용자가 명시적으로 지정한다.
2. **계획 먼저**: 코드를 작성하기 전, 어떤 파일/디렉토리를 만들지, 어떤 순서로
   작업할지 계획을 markdown으로 먼저 제시하고 승인을 받은 후에 실제 작업을 시작한다.
3. **리스크 스파이크 우선**: `PROJECT_PLAN.md`의 "검증이 필요한 리스크" 항목은
   본 기능 구현 전에 최소 코드로 먼저 검증한다. 검증 없이 그 위에 기능을 쌓지 않는다.
4. **한 번에 한 걸음**: 여러 단계를 한꺼번에 진행하지 않는다. 각 단계가 끝나면
   멈추고 사용자의 확인을 기다린다.
5. **기록**: 기술 선택, 롤백, 발견한 이슈는 `DECISION_LOG.md`에 1~2줄로 남긴다.
6. **empirical 검증**: "이게 맞을 것 같다"가 아니라 실제로 실행/테스트한 결과로
   판단한다. 확신이 없으면 먼저 최소 재현으로 확인한다.